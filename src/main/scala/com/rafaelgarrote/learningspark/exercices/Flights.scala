package com.rafaelgarrote.learningspark.exercices

/**
 * Created by rgarrote on 30/09/15.
 */
object Flights {
//
//  def parse(l:String, position:Int):String = { try {position match {case 19|20|23|8|16|17 => l; case _=> java.lang.Integer.parseInt(l);l } } catch {case e: Exception => "@" }}
//  val arroba = data.mapPartitionsWithIndex { (idx, iter) => if (idx == 0) iter.drop(1) else iter }.map(_.split(",")).map(l => l.zipWithIndex.map(c => parse(c._1, c._2)))
//  arroba.reduce(_ ++ _).filter(_.contains("@")).length
//
//
//  def range(l:String, position:Int):String = {position match{case 21|24|25|26|27|28 => (l == 0 || l == 1).toString; case _ => l}}
//  arroba.filter(c => !c._1.contains("@")).map(c => range(c._1, c._2)).reduce(_ ++ _).filter(_.contains("false")).length

}
